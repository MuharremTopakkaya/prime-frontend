{
  "version": 2,
  "buildCommand": "npm install && cd apps/admin && npm run build",
  "outputDirectory": "apps/admin/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

